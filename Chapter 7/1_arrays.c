//collection of simillar data types stored at contiguos(continuos) memory location
// syntax -
// int a[] , char a[] , float a[]  
// [] = size of array (per block 4 bytes)
// for giving i/p to array: scanf("%d",&marks[0]) [0] means storing that value at 0th position
// for taking o/p : printf("%d",marks[0]) accessing the value with is stored at 0th pos in array
// positions always start from 0 in arrays
