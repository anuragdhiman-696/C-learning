/* if we wanna convert float to int thro compiler then we have to give some command */
#include<stdio.h>
int main () {
    // int a = (int) 1.999999;
    // printf("%d",a);
    float a = (float) 1;
    printf("%f",a);
}
// this will remove the digits after decimal