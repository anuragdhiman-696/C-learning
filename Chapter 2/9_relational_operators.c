#include<stdio.h>
int main() {
// if we wanna check if a = b or not then we have to use 2 equal signs between them (== means equal to)
printf("%d \n", 3==2);
printf("%d \n", 4<=4 );
printf("%d \n", 3!=2); // != means not equal to
return 0;
}
// 1 means true , 0 means false