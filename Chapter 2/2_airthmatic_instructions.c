//1. airthmatic means algebra of variables
//2. only single variable on left side is valid
// valid : a=b+c , invlid : b+c=a
/*3. if we want b to the power c then b^c is wrong we have to use a pow() function and we have to include<math.h> with power function i.e
{#include<stdio.h>
#include<math.h>
int b,c;
b=c=1;
int power = pow(b,c);}     */
