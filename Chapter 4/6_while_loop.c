// we have to declare the int befor while loop 
// and the updation ondition is in the end inside while loop
#include<stdio.h>
int main () {
    int i=1;
    while(i<=5){ // for infinite loop just write (i)
        printf("Hello World \n");
        i++;
    }
    return 0;
}